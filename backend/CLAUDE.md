# DJ Cloud Backend

## API documentation

`API.md` (in this folder) documents every REST endpoint for frontend consumers. Whenever an endpoint is
added, removed, or its request/response shape, auth requirement, validation, or status codes change,
update `API.md` in the same change — don't let it drift out of sync with the actual controllers.
